;; This file was autogenerated by Puppet. Changes will be overwritten on next puppet agent run.

[pgbouncer]

<% @class_config_parameters.each do |category, parameters|
  next if parameters.reject { |_, value|
    ['undef', :undef].include?(value) || value.to_s.empty?
  }.empty?
-%>

;;
;; <%= category %>
;;
  <%- parameters.each do |parameter, value|
    next if ['undef', :undef].include?(value) || value.to_s.empty?
  -%>
<%= parameter %> = <%= value %>
  <%- end -%>
<%- end -%>

;;
;; Parameters passed via raw_config:
;;

<% @raw_config.each do |parameter, value| -%>
  <%- next if ['undef', :undef].include? value -%>
<%= parameter %> = <%= value %>
<% end -%>

%include <%= @db_config_file %>
